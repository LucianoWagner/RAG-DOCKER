# =============================================================================
# Dependencias para Evaluación RAGAS (requiere Python 3.11 o 3.12)
# =============================================================================
# Instalar en venv-eval: pip install -r requirements-eval.txt
# =============================================================================

# --- RAGAS Framework ---
ragas>=0.2.0
datasets>=2.19.0
pandas>=2.2.0
tabulate>=0.9.0

# --- LLM Providers (juez + generador) ---
langchain-groq>=0.1.0
langchain-ollama>=0.3.0
openai>=1.0.0

# --- LangChain Core (necesario para wrappers RAGAS) ---
langchain>=0.3.14
langchain-core>=0.3.30
langchain-community>=0.3.14
langchain-text-splitters>=0.3.0

# --- Vector Store & Retrieval (para correr el pipeline) ---
langchain-chroma>=0.2.2
chromadb>=0.6.3
rank-bm25>=0.2.2
flashrank>=0.2.9

# --- Config & Utils ---
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
loguru>=0.7.3
httpx>=0.28.0
langdetect>=1.0.9
deep-translator>=1.11.0
